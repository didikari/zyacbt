# ZYA CBT Environment Configuration

# Application Environment (development, testing, production)
CI_ENV=development

# Base URL (leave empty for auto-detection)
BASE_URL=

# Database Settings
DB_HOSTNAME=localhost
DB_USERNAME=root
DB_PASSWORD=root
DB_DATABASE=zyacbtpublic

# Encryption Key (leave blank to use the default legacy fallback)
ENCRYPTION_KEY=
